# ===== courser 敏感配置模板 =====
# 复制为 .env（.env 已被 .gitignore 忽略），或直接在 TUI「设置」里填写。

# ---- PKU 学号/密码（可选）----
# 不配置则依赖浏览器密码管理器自动填充（打开登录页等待一小会直接点登录）。
# 注意：在 opencli 自动化窗口中，少数环境密码管理器不会自动填充，
# 此时必须在这里（或 TUI 设置）配置，脚本会先填好再点登录。
# PKU_USERNAME=
# PKU_PASSWORD=

# ---- 邮件通知（gws = Google Workspace CLI）----
# 发送邮件走 gws，需先安装并授权（仅一次）：
#   brew install gws    或  npm i -g @googleworkspace/cli
#   gws auth login      （浏览器完成 OAuth2 授权）
# 收件邮箱在 TUI「设置 → 邮件通知」填写（也可用环境变量覆盖）：
# MAIL_TO=you@example.com
# GWS_FROM=            # gws 发件账号（Gmail 地址，可选，默认取认证账号）